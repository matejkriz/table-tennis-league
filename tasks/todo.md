# TODO - Current PR Comment Follow-up

- [x] Verify every current PR comment against the latest branch state.
- [x] Add failing regression tests for roster reconciliation and controlled collapsible behavior.
- [x] Fix valid code issues in route reconciliation, collapsible control handling, and hook docs/logging.
- [x] Re-check whether the MatchRecorder drift comment reproduces after fixes and only change it if needed.
- [x] Run targeted tests, `tsc --noEmit`, and record the results in task docs.
